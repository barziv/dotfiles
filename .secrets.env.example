# Template for .secrets.env (which is gitignored).
# setup.py reads this file to know what secrets exist, then writes the real
# values into .secrets.env after prompting you. Copy this to .secrets.env
# and fill in real values if you want to skip the interactive prompt.

CONTEXT7_API_KEY=""
